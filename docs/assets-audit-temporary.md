# Auditoría temporal de imágenes

Esta rama genera `assets-inventory.json` y `assets-audit.html` durante el Deploy Preview para revisar visualmente los recursos existentes en `assets/`.

Los archivos son temporales, usan `noindex` y no deben fusionarse en producción.
